---
import { partners } from '@/data/partners';

// Duplicate partners for seamless infinite scroll
const duplicatedPartners = [...partners, ...partners, ...partners];
---

<section class="section relative">
  <div class="container-custom relative z-10">
    <div class="text-center mb-8">
      <h2 class="section-title text-white">Đối tác chiến lược</h2>
      <p class="text-secondary-300 mt-2">Hợp tác cùng các chủ đầu tư hàng đầu Việt Nam</p>
    </div>

    <!-- Infinite scroll container -->
    <div class="partners-slider overflow-hidden relative">
      <div class="partners-track flex items-center gap-8">
        {duplicatedPartners.map((partner) => (
          <a
            href={`/chu-dau-tu/${partner.slug}`}
            class="partner-item flex-shrink-0 bg-white rounded-xl p-4 shadow-xs hover:shadow-md transition-shadow"
          >
            <img
              src={partner.image}
              alt={partner.name}
              class="h-16 w-auto object-contain"
              loading="lazy"
            />
          </a>
        ))}
      </div>
    </div>
  </div>
</section>
